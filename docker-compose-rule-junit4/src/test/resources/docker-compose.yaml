db:
  image: appropriate/nc
  command: /bin/sh -c 'echo server started && nc -lk 5432'
  ports:
    - "5432"
  stop_signal: SIGKILL

db2:
  image: appropriate/nc
  command: /bin/sh -c 'echo server started && nc -lk 5432'
  ports:
    - "5432"
  stop_signal: SIGKILL

db3:
  image: appropriate/nc
  command: /bin/sh -c 'echo server started && nc -lk 5432'
  ports:
    - "5432"
  stop_signal: SIGKILL

db4:
  image: appropriate/nc
  command: /bin/sh -c 'echo server started && nc -lk 5432'
  ports:
    - "5432"
  stop_signal: SIGKILL
